package com.mrpup.clumapi.renders;

/*
public class ComponentBlockEntityRenderer implements BlockEntityRenderer<ComponentBlockEntity> {
    public ComponentBlockEntityRenderer(BlockEntityRendererProvider.Context context) {

    }

    @Override
    @SuppressWarnings("unchecked")
    public void render(ComponentBlockEntity blockEntity,
                       float partialTick,
                       PoseStack poseStack,
                       MultiBufferSource bufferSource,
                       int packedLight,
                       int packedOverlay) {

        if (!blockEntity.hasRenderer()) return;

        IBlockRenderer<ComponentBlockEntity> renderer =
                (IBlockRenderer<ComponentBlockEntity>) blockEntity.getRenderer();

        renderer.render(blockEntity, partialTick, poseStack, bufferSource, packedLight, packedOverlay);
    }
}

*/

